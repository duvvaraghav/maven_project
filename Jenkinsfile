// Declarative pipeline
pipeline {
    agent none

    stages {
	stage('Setting the variables values') {
            steps {
           	 sh '''
                    #!/bin/bash
                    echo "hello world"
                 '''
             }
        }
    }
}
